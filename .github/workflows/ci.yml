# Stub: lint + test on every PR and push to main. See docs/build-plan.md (Phase 14).
# Implemented per architecture-design.md §12.1:
#   backend-test   — postgres service; ruff check; mypy app/; pytest
#   frontend-test  — eslint; tsc --noEmit; pnpm test
#   frontend-build — pnpm build
