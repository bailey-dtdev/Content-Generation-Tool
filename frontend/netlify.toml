# Stub: Netlify config. See docs/build-plan.md (Phase 15).
# Full config in architecture-design.md §11.2: pnpm build, publish dist/,
# /api/* proxy to the Fly backend, SPA fallback redirect to /index.html.
