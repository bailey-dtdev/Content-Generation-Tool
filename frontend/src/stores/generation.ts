// Stub: Zustand generation store — in-progress generation, persisted to
// sessionStorage. The only place generation content lives (backend does not
// store it). See docs/build-plan.md (Phase 8) and architecture-design.md §7.2.
export {};
