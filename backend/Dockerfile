# Stub: backend container image. See docs/build-plan.md (Phase 1).
# Implemented per architecture-design.md §11.1: multi-stage build (uv installs deps,
# runtime is python:3.12-slim with the venv copied in).
# Final CMD: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
