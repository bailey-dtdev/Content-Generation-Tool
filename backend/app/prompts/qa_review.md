---
id: qa_review
version: 0
description: LLM QA pass — shared across all content types. Returns a JSON array of QA notes.
variables:
  - client
  - content_sections
  - rule_based_findings
---

<!-- Stub: placeholder prompt body. See docs/build-plan.md (Phase 10). -->
<!-- Prompt content is collaborative IP — iterated with the team, not pre-written. -->
<!-- See architecture-design.md §6.10.2 and §16. -->
<!-- Must instruct the model to return JSON only — validated against a Pydantic schema. -->

Placeholder body for the shared QA review prompt.
